{
  "name": "bsale-proxy",
  "version": "1.0.0",
  "description": "Proxy backend para BSale API",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build"
  },
  "dependencies": {
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "22.x"
  }
}
